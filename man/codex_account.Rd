% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{codex_account}
\alias{codex_account}
\title{Show a redacted Codex authentication summary.}
\usage{
codex_account(auth = NULL)
}
\arguments{
\item{auth}{Optional in-memory credential. If omitted, the current
process-local session and then the package's own credential store are
checked.}
}
\value{
A data frame with authentication state, a redacted account field,
and the access-token expiry time.
}
\description{
The account identifier and all token material are deliberately replaced or
omitted. Calling this function does not refresh a credential or contact the
network.
}
\section{Conditions}{

A malformed in-memory credential signals \code{codex_auth_argument_error} and a
malformed stored value signals \code{codex_credential_store_error}.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
codex_account()
\dontshow{\}) # examplesIf}
}
