% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{codex_available}
\alias{codex_available}
\title{Check whether the local ellmercodex integration is available.}
\usage{
codex_available(check_credentials = FALSE)
}
\arguments{
\item{check_credentials}{Whether to inspect this package's own credential
backend. Defaults to \code{FALSE} to keep ordinary availability checks free of
credential-store I/O.}
}
\value{
A single logical value. The full redacted diagnostic object is
available internally through \code{codex_diagnostics()}.
}
\description{
This is an offline check. By default it verifies package dependencies and
static configuration only; it does not open a browser, make a request, or
inspect a credential store. Set \code{check_credentials = TRUE} when an explicit
credential-presence check is desired.
}
\examples{
codex_available()
if (interactive()) codex_available(check_credentials = TRUE)
}
