% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat-codex.R
\name{chat_codex}
\alias{chat_codex}
\title{Create an experimental Codex chat backed by ellmer}
\usage{
chat_codex(system_prompt = NULL, model = NULL, echo = c("none", "output"))
}
\arguments{
\item{system_prompt}{Optional system prompt passed to ellmer.}

\item{model}{Optional Codex model name. If omitted, the package default is
used.}

\item{echo}{One of \code{"none"} or \code{"output"}; controls whether \verb{$chat()}
prints the completed text. The compatibility wrapper accepts \code{TRUE},
\code{FALSE}, and \code{"text"} as aliases for \code{"output"}, \code{"none"}, and
\code{"output"}, respectively.}
}
\value{
An object inheriting from ellmer's \code{Chat} class.
}
\description{
\code{chat_codex()} returns a normal ellmer \code{Chat} object configured for the
observed Codex subscription transport. It does not start browser
authentication. Call \code{\link[=codex_login]{codex_login()}} explicitly first when no stored
credential is available; an existing credential is loaded and refreshed as
needed.
}
\details{
The returned chat supports ordinary text \verb{$chat()}, text or content
\verb{$stream()}, and multi-turn history. The compatibility layer buffers the
public ellmer stream and repairs the final assistant turn because the
observed Codex terminal event can omit the output that was present in its
preceding deltas. Structured output, asynchronous methods, and tool calling
are outside this experimental interface.
}
\section{Conditions}{

Invalid arguments signal \code{codex_chat_argument_error}. Missing or
incompatible ellmer installations signal \code{codex_ellmer_missing} or
\code{codex_ellmer_compatibility_error}. Authentication failures found before
the Chat is constructed retain their package condition classes; errors while
delegating through the ellmer seam are sanitized as \code{codex_chat_error}.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
chat <- chat_codex(
  system_prompt = "Be concise.",
  model = Sys.getenv("ELLMERCODEX_MODEL", "gpt-5.6-luna")
)
chat$chat("Hello")
\dontshow{\}) # examplesIf}
}
